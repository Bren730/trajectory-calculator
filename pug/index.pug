doctype html

html
	head
	meta(charset='utf-8')
	meta(name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=0")
	title
	link(href='/static/css/app.css' rel='stylesheet' type='text/css')
	link(href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i,800,800i&amp;subset=greek,latin-ext" rel="stylesheet")

body(ontouchstart)
	.content
		h1 Discus trajectory calculator

		section.vacuum.model
			h2 Perfect vacuum
			svg(id='vacuum-model-svg')

			.variables
				.variable
					label v#[sub release]
					input(id='vacuum-v0' model='vacuum')
				.variable
					label &theta;#[sub release]
					input(id='vacuum-thetaRelease0' model='vacuum')
				.variable
					label g
					input(id='vacuum-g' model='vacuum')
				.variable
					label release height
					input(id='vacuum-y0' model='vacuum')
				.variable
					label &Delta;t
					input(id='vacuum-deltaT' model='vacuum')

			button(id='vacuum-calculate') Calculate

		section.air-resistance.model
			h2 Air resistance
			svg(id='air-resistance-model-svg')

			.variables
				.variable
					label v#[sub release]
					input(id='air-resistance-v0' model='air-resistance')
				.variable
					label &theta;#[sub release]
					input(id='air-resistance-thetaRelease0' model='air-resistance')
				.variable
					label &theta;#[sub attack]
					input(id='air-resistance-thetaAttack0' model='air-resistance')
				.variable
					label v#[sub wind]
					input(id='air-resistance-vWind' model='air-resistance')
				.variable
					label g
					input(id='air-resistance-g' model='air-resistance')
				.variable
					label release height
					input(id='air-resistance-y0' model='air-resistance')
				.variable
					label &Delta;t
					input(id='air-resistance-deltaT' model='air-resistance')
				.variable
					label &rho;
					input(id='air-resistance-rho' model='air-resistance')
				.variable
					label Mass
					input(id='air-resistance-m' model='air-resistance')

			button(id='air-resistance-calculate') Calculate
			a(id='air-resistance-save' href='' target='_blank') Save

	script(src='static/js/vendor.js')
	script(src='static/js/app.js')